name: test-rhad

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  test-rhad:
    name: ${{ matrix.config.os }}
    runs-on: ${{ matrix.config.os }}

    strategy:
      matrix:
        config:
          - {os: ubuntu-latest}

    steps:
      - uses: actions/checkout@v2
      - name: Sysinit
        run: sudo bash scripts/init.sh
      - name: Test
        run: make test
